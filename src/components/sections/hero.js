import React from "react";
import styled from "styled-components";

const StyledSection = styled.section``;

const Hero = () => {
  return <StyledSection>Hero</StyledSection>;
};

export default Hero;
