import React from "react";
import styled from "styled-components";

const StyledSection = styled.section``;

const Projects = () => {
  return <StyledSection id='projects'>Projects</StyledSection>;
};

export default Projects;
