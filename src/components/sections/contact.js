import React from "react";
import styled from "styled-components";

const StyledSection = styled.section``;

const Contact = () => {
  return <StyledSection id='contact'>Contact</StyledSection>;
};

export default Contact;
